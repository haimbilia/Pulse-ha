# Pulse Home Assistant Integration

HACS-installable custom integration scaffold for Pulse.

## Structure

- `custom_components/pulse/` - Home Assistant integration
- `hacs.json` - HACS metadata
- `.github/workflows/` - validation workflows

## Status

This is a starter scaffold. The Pulse API calls in `api.py` are placeholders and should be wired to your actual Pulse service.
